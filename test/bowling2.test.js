const Game = require('../src/bowling2/index');
const { globalAgent } = require('http');
const assert = require('assert').strict;

describe("bowling: new game", function() {
 it.skip("should be able to instantiate a new game", 
});

describe("bowling: roll(pins)", function() {
  it("should be able to add a roll to a frame", function() {

  });
});